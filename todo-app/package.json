{
  "name": "todo-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
      "server": "json-server --watch data/db.json --port 3000 --delay 0"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "axios": "^1.7.7"
  }
}
